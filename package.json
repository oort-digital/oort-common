{
  "name": "oort",
  "version": "0.0.1",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "sites/*"
    ]
  },

  "scripts": {
    "rm": "rimraf node_modules && rimraf ./packages/common/node_modules && rimraf ./sites/my-site/node_modules"
  }
}